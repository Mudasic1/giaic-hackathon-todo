# Task Breakdown

- [ ] **T-001**: Set up project structure with `uv` and create `src/models.py` for the Task data class.
- [ ] **T-002**: Implement `TaskManager` in `src/manager.py` with Add and View methods.
- [ ] **T-003**: Implement Update, Delete, and Complete methods in `TaskManager`.
- [ ] **T-004**: Create `src/main.py` to handle the CLI loop and user interaction.
- [ ] **T-005**: Final polish: Add error handling for non-existent IDs.